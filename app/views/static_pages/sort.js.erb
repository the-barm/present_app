$(function() {
  $('div.colors').remove();
  var state = <%=raw @colors%>;
  for (var i = 1; i < <%=@colors.length %>+1; i++) {
    $('div#array-background').append($('<div/>', {id: 'color-'+i, class: 'colors'}));
    $('#color-'+i).css("background-color", state[i-1]);
  };
});

