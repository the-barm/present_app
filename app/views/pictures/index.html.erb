<% provide(:title, 'All pictures') %>

<% if logged_in? %>

<h1>All pictures</h1>

<ul id="pictures">
  <% @pictures.each do |picture| %>
    <li>
      <%= image_tag(picture.data.url) %>
    </li>
  <% end %>
</ul>

<% end %>
